# Local dev uses the Vite proxy in client/vite.config.ts.
VITE_API_BASE_URL=/api

# Vercel production:
# - Keep /api if Vercel rewrites /api/* to Render.
# - Without a rewrite, set this in the Vercel dashboard to:
#   https://your-render-service.onrender.com/api
