{
  "name": "quis-ux-survey-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "lint": "npm -w server run lint && npm -w client run lint",
    "format": "prettier -w .",
    "format:check": "prettier -c ."
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.6.2"
  }
}

